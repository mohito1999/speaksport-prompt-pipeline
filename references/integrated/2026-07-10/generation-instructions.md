# Integrated generation instructions

Status: active. Owner-confirmed through 2026-07-22.

Generate facility-specific content using the selected reference for architecture and depth, never as an authoritative source of facility facts.

The integrated reference's operating structure is mandatory. Preserve its major headings, explicit protocols, stop-and-wait instructions, date context, caller-detail confirmation depth, tool failure handling, and closing core-shell architecture. Adapt facility facts and configured destination identifiers, but do not summarize, soften, redesign, or replace those conventions with generic prose. The reference's first-request Golf Shop deflection is conditional rather than universal and is controlled only by the facility transfer policy below.

- Return named sections for the core shell, knowledge base, logic module, and any intentional closing core-shell blocks.
- Initialize only the integrated reference variables: `{{phone_recognized}}`, `{{first_name}}`, `{{last_name}}`, `{{email}}`, `{{caller_is_customer}}`, `{{customer_passes}}`, `{{customer_groups}}`, `{{price_class}}`, `{{customer_has_card_on_file}}`, and `{{courses}}`. Use `{{greeting}}`, `{{disclaimer}}`, and `{{announcement}}` exactly in the greeting flow. Do not initialize variables merely because they exist in a global payload or historical guide.
- Include this exact date-and-time sentence, substituting the facility's configured IANA timezone in both expressions: `Today is {{"now" | date: "%A, %B %d, %Y", "<facility timezone>"}}, and the current time is {{"now" | date: "%I:%M %p", "<facility timezone>"}}.` Preserve the speaking-date rules and nearest-future interpretation of “next Tuesday.”
- Preserve the full two-step transfer protocol, explicit stop-and-wait behavior, and prohibition against confirmation and transfer in the same response.
- Read `transfer_policy.first_shop_transfer_deflection` from the facility configuration. If it is `true`, include the first-request Golf Shop or Pro Shop deflection guardrail and its approved “busy with guests, let me help first” behavior. If it is `false`, omit that guardrail completely: do not resist, delay, deflect, or gatekeep a caller's first shop-transfer request. Instead, immediately use the normal two-step transfer protocol by offering the transfer, stopping for confirmation, and transferring after the affirmative reply.
- When enabled, preserve `get-day-of-week-staging`, `fetch-inventory-for-date`, and `get-weather-forecast-staging` with the same operational depth as the reference.
- Keep facts in the knowledge base, global receptionist behavior in the core shell, and workflows in the logic module.
- Treat the approved fact inventory as a coverage checklist, not source material to summarize aggressively. Include every caller-useful informational fact supported by it. Preserve every distinct membership tier, price, spouse or junior price, benefit, booking window, discount, rate, hour, date range, season, age threshold, fee, tax, gratuity, capacity, amenity, staff role, course detail, policy, exception, restriction, penalty, event term, instruction detail, historical fact, and accolade. Never replace detailed supported options with phrases such as “various memberships,” “rates vary,” or a representative sample when the inventory contains the specifics.
- Organize and deduplicate the knowledge base for natural lookup, but do not merge facts when doing so loses conditions or distinctions. Rich facilities should produce a correspondingly rich knowledge base; 1,500 to 3,500 words or more is normal when the evidence supports it. Never pad or invent details merely to reach a length.
- Preserve membership, pass, golf-rate, booking-policy, facility-policy, staff, instruction, and operational details at high resolution. For long catering menus, repeated event calendars, hole-by-hole narratives, or other bulk reference material, retain useful package names, prices, fees, capacities, dates, and representative or defining inclusions without reproducing every dish, duplicate event occurrence, or source-table cell. This is loss-aware consolidation, not permission to reduce major facility offerings to generic summaries.
- Official-site facts are approved informational evidence unless contradicted by a higher-authority source or explicitly listed in `ignored_facts`. Retain changeable information and label it as “published,” “listed,” or with its supplied year or season when appropriate. Do not move supported facts into “open questions” merely because current confirmation could be useful.
- Omit phone numbers from the prompt under the current global convention even if they appear in the fact inventory. Omit source provenance and internal confidence labels from caller-facing knowledge.
- Before returning, audit the completed knowledge base against the fact inventory item by item. Use `generation_notes` to state the inventory fact count and enumerate every caller-useful fact intentionally omitted with the exact reason. Do not silently omit facts to shorten the prompt.
- Invoke `check-booking-eligibility-staging` as soon as every input actually used by the facility's booking eligibility policy is known. Date and approximate time are always collected first. If and only if the eligibility policy evaluates `num_players`, ask for player count before the eligibility call and pass it as `num_players`. Otherwise, do not collect player count before eligibility. Never collect holes, riding or walking, identity, email, or inventory before eligibility succeeds. Pass `course_name` before eligibility only when an approved multi-course eligibility rule explicitly requires it.
- Treat player count and hole count as mandatory availability inputs even though the backend schema accepts them as optional. Before every `get-available-tee-times-staging` call, the caller must have explicitly provided `num_players` and selected `num_holes` as 9 or 18. Make this a mandatory stop in the receptionist workflow. Every availability call must pass `date`, `num_players`, `num_holes`, and an exact 24-hour `HH:MM` value in `when` when a clock time is known; also preserve the configured multi-course filter or single-course omission. Never search availability after asking only for holes while player count remains unknown.
- Availability returns only a small set of nearest-time matches around the supplied `when`; it is not an exhaustive list of the day's inventory. If the caller asks whether a different exact time is available—including a time they saw online that was absent from the prior result—immediately call `get-available-tee-times-staging` again. Preserve the same date, `num_players`, `num_holes`, and course filter or omission, but replace `when` with the newly requested exact time converted to 24-hour `HH:MM`. Do not say that the requested time is unavailable, do not claim the website differs from the tool, and do not offer transfer or online booking until this targeted re-query returns.
- Treat availability results as inseparable time/course pairs and pass the exact selected pair to `book-tee-time-staging`.
- For a single-course facility, normally omit the availability course filter, then pass the returned slot's exact course when booking.
- For a multi-course facility, ask which exact course from the initialized `{{courses}}` list the caller wants, pass it as `course_name` to availability, and pass the same selected/returned course value when booking.
- Never state booking success before the booking tool confirms it.
- Enabled tool capabilities override the older reference facility's scope statement about cancellations. If `get-bookings` is enabled, state that the assistant can check and confirm existing reservations. If `get-bookings`, `get-eligibility-for-cancellation`, and `cancel-reservation` are all enabled, state that the assistant can also cancel eligible reservations. Never claim either capability when its required tools are disabled. Booking modifications, player-count changes, rescheduling, and time changes remain transfer-only.
- When `get-bookings` is enabled, include a complete **Checking Existing Bookings** workflow. First call `get-bookings` with no parameters so it searches by the caller's phone number. If bookings are found, speak only the date, naturally formatted time, number of players, and course name; never reveal, recite, spell, or otherwise expose a booking reference. If none are found, explain that nothing was found under the caller's phone number, ask whether they have a booking reference, mention the staff-transfer alternative, then stop and wait. If the caller supplies a reference, call `get-bookings` again with only `booking_reference`; never pass `course_name`. ForeUp references must begin with `TTID_`, so restore that prefix when transcription clearly omitted or corrupted it. If the fallback also returns nothing, offer the configured golf-operations transfer using the standard Two-Step Transfer Process.
- When the full cancellation tool set is enabled, include a complete **Cancellations** workflow in this exact order: use the booking-lookup sequence; present reservation details without references; ask which exact tee time the caller wants to cancel and stop; preserve the hidden reference paired with each result; call `get-eligibility-for-cancellation` using the selected reservation's exact `date` and `time`; if `eligible` is false, stop, speak the exact returned `reason`, and offer the configured golf-operations transfer under the Two-Step Transfer Process; if `eligible` is true, immediately call `cancel-reservation` with only the exact hidden `booking_reference` associated with the selected reservation. Confirm cancellation only after the cancellation tool reports success. If it does not report success, do not imply that the reservation was cancelled and follow the approved tool-failure recovery flow.
- Do not ask for an additional cancellation confirmation after eligibility returns true; the caller's explicit selection of the tee time to cancel is the cancellation instruction. Keep booking references internally for tool calls only.
- Mention only logical tool names from the active contract.
- Do not include eligibility decision rules in the receptionist prompt.
- Generate a separate booking eligibility decision prompt using the exact format in `eligibility-conventions.yaml`: variable initialization first, then concise ordered rules. Use single-quoted machine names only in initialization. In all later rules, refer to their meanings in natural language without quotes, such as “requested booking date,” “number of players,” or “customer's card on file status.” Preserve owner-supplied reason strings exactly and finish by forbidding every non-approved eligibility criterion.
- When `get-eligibility-for-cancellation` is enabled, also generate a separate cancellation eligibility decision prompt. Initialize reservation date, reservation tee time, facility-local current date, facility-local current datetime, and server-computed hours until tee off. Prefer hours until tee off for cutoff comparisons. Preserve the configured cutoff, exact reasons, inclusive boundary, and explicit prohibition against all other cancellation criteria. Never combine booking eligibility and cancellation eligibility in one artifact.
- Use `transfer_call-staging` with exact configured destination identifiers and no phone numbers.
- Do not copy names, prices, destinations, phone numbers, events, exceptions, or test behavior from the reference facility.
- Surface conflicts and missing inputs as open questions instead of inventing an answer.
